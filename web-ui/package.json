{
  "name": "dax-catalog-orchestrator-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight web UI for the AgentOrchestrator pipeline",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "build": "node -e \"console.log('No build needed here. Run `npm run build` in project root to compile orchestrator to dist/.')\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dax-catalog-mvp": "file:..",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  }
}
